# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-api-key-here

# ChromaDB Server Configuration
# Host and port for the ChromaDB server
CHROMA_SERVER_HOST=0.0.0.0
CHROMA_SERVER_PORT=8000

# GitHub Configuration
# Personal Access Token for GitHub authentication
GITHUB_TOKEN=your-github-token-here

# Next.js Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api
PORT=3000

# Docker Configuration
# Docker will use these values when running the container
DOCKER_CHROMA_DATA_DIR=/app/chroma-db
DOCKER_LOGS_DIR=/app/logs
DOCKER_NODE_ENV=production

# Docker Compose Configuration (if using docker-compose)
# COMPOSE_PROJECT_NAME=realtime-agent

# Backup Configuration (optional)
# BACKUP_DIR=/backups
# BACKUP_RETENTION_DAYS=7
